# 简历项目经历初稿 v1

> 生成日期：2026-08-11
> 写作风格参考：06-external-internship-reference.md（懂车族/京东密集段落式）

---

## 实习经历

实习经历
广州博冠信息科技有限公司（网易互娱） | AI Lab  DreamMaker | AI应用开发实习生 | 2026.03 - 2026.08
负责 AI 美术智能体平台的 Agent 业务层开发与底层分布式调度系统建设，覆盖图像 / 视频 / 3D 等创作能力落地
模块一：Monet AI Agent画布（Python / FastAPI / LangGraph / Asyncio）
项目背景：网易互娱AI美术平台客户端Agent服务，通过与画布UI交互，驱动图像/视频/3D 等 AI 创作能力，让游戏美术师从“亲手画画”转变为“驾驭AI Agent完成操作”。
	分层上下文感知架构设计：针对 LLM 无法感知动态画布、全量画布硬编码 Prompt 引发 Token 膨胀问题，设计三层分层上下文注入架构。（1）入口层：在请求中间件通过ContextVar做会话隔离，预提取轻量化画布概览写入请求缓存；（2）推理拦截层：每次 LLM 调用前自动注入最新画布快照，深拷贝隔离历史状态，杜绝上下文污染；（3）工具检索层：开放画布细节查询工具，Agent 按需调取精细节点信息，复用缓存降低重复查询开销。该方案替代静态 System Prompt硬编码，支持画布动态刷新，显著降低常驻上下文体量，基于10个不同复杂度画布样本测算，按预估线上场景分布加权，画布相关输入Token开销下降约40%。
	对话状态持久化：基于 LangGraph Checkpointer 机制实现本地 SQLite 持久化，支持 Agent 对话的断点恢复：每次状态转换自动写入 checkpoint，客户端重启或断连后从最近检查点恢复上下文；设计会话级任务取消机制，同一 session 新请求到来时自动取消旧任务并等待其安全退出，保证 checkpoint 状态一致。
	SSE 流式协议设计：设计 Agent 到前端的标准化SSE 流式通信协议：抽象流式适配器基类，封装事件 ID 自增、长连接心跳保活、异常帧自动发送等通用能力；实现具体适配器将 LangGraph 内部事件统一序列化映射为 Message → Block 双层内容模型，支持文本、工具调用等类型Block，采用Delta 增量分片推送。前端仅监听标准化 SSE 事件即可渲染，实现底层Agent框架无感切换，大幅降低前后端联调成本。
	Human-in-the-Loop权限审批：基于 LangGraph原生interrupt机制搭建HITL审批引擎，自定义权限分级协议：将工具行为划分为只读操作（自动放行）、写入 / 命令高危操作（强制人工审批）；Agent 触发审批节点时主动挂起执行，经由 SSE 向前端推送标准化审批事件；用户确认指令通过 REST 接口回传，驱动 Graph resume 恢复任务执行；协议抽象支持确认框、多选项等多种交互类型拓展。


模块二：DreamMaker 分布式 AI 任务调度平台（Go / Redis / etcd / MongoDB / K8s）
项目背景：网易互娱平台AI美术平台主站后端，五层微服务架构（AI网关->业务->调度->Worker->供应商），承载图像/视频/3D/音频等 10类AI任务的调度与执行。
	分布式优先级调度：基于 Redis ZSet 搭建分布式优先级任务队列，采用ZRange+ZRem无锁乐观抢占消费方案，规避分布式锁带来的性能损耗；参与实现加权轮询调度算法，按配额分配高低优先级算力，配额耗尽启用降序兜底调度，调度吞吐量提升约 25%，杜绝低优先级任务饥饿问题；基于etcd Watch 监听 + 内存原子计数器实现分布式用户并发管控，限制单用户最大并行任务上限，防止个别用户独占集群算力资源。
	高可用弹性扩缩容：解决 Worker 节点单点故障、算力资源浪费的问题，基于 etcd Lease+Watch 落地服务注册与心跳检测机制，30s心跳间隔，调度中心通过 etcd Watch 实时监听节点上下线，租约过期自动标记失效Worker；搭建自定义扩缩容模块，按队列积压调用 K8s API 动态扩缩容 StatefulSet节点，集群空闲算力占用降低约 35%。
	供应商容错降级：针对第三方 AI 供应商 429 限流导致任务直接失败的痛点，设计排队+指数退避重试机制，触发429 后进入阶梯退避重试，超时未恢复则抛出可重试异常，自动切换备用供应商链路兜底；支持白名单精细化管控生效范围，高并发场景下任务失败率降低约 50%，减少业务阻塞延迟。



科大讯飞股份有限公司 | 多模理解组 | 大模型算法实习生 | 2025.05~2025.09
项目背景：借鉴 DeepSeek-R1 深度慢思考思想，针对多模态大模型常规快速推理存在逻辑断层、事实幻觉的缺陷，参与 13B 星火多模态大模型 SFT 后训练优化，聚焦长思维链推理能力提升，覆盖数据集构建、微调与自动化评测全流程。

	长 CoT 数据集构建：搭建标准化数据生产流水线，聚焦STEM、视觉推理等高难度场景整合开源数据源；通过提示词工程批量生成[快思考-慢思考]配对样本，采用规则校验+大模型交叉核验方案校验答案与推理链一致性、落地拒绝采样、难度分层标注，脏数据清洗流程；最终产出 30w + 高质量多模态长 CoT 训练样本。
	轻量化微调与评测：对讯飞13B星火多模态大模型开展监督微调（SFT）；使用 LoRA 低秩适配器做轻量化微调，降低算力开销、提升迭代速度；基于 vLLM+FastAPI 搭建高性能离线推理服务，在 MathVista、MMVet 主流多模态权威benchmark搭建自动化评测链路，量化验证长 CoT 方案优化效果。优化后的 13B 多模态模型，MathVista 指标相对提升 14.6%，MMVet 指标相对提升 16.4%。
